# Image file for CI image.
This image should use as slave (Jenkins) or Runner (Gitlab),  
and use docker in docker (dind)

## Build Command
docker build -t devopsteamelt/dind:base_1.2 .  
The "***1.2***" is tag, and should update.

